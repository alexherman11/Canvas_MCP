# Canvas LMS API Configuration
# Get your API token from: Canvas → Account → Settings → New Access Token

# Required for local stdio mode. Optional for remote mode (credentials come via headers).
CANVAS_BASE_URL=https://canvas.yourschool.edu
CANVAS_API_TOKEN=your_token_here

# Port for remote HTTP mode (Railway sets this automatically)
# PORT=3000
